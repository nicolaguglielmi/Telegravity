---
description: Enter Telegravity Active Mode — park on Telegram and act on incoming instructions
---

Enter Telegravity Active Mode now: load the `telegravity-active-mode` skill
bundled with this plugin and follow its loop until the user says stop.

If the Telegravity MCP tools are not available, tell the user the Telegravity
MCP server is not connected (it needs `pip install telegravity`, plus
`TELEGRAM_TOKEN` and `AUTHORIZED_CHAT_ID` in `~/.telegravity/.env`), and stop.
